# This repository contains a context manager that opens and reads multiple files at once.
# It is designed to safely close all files even when it encounters issues.
# It also includes a batching iterator used to stream parts of CSVs

# The goal of this repository is to be a proving ground for my own implementations of maintainable IO
